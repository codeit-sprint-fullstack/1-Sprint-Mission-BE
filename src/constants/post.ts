const MIN_LENGTH_NAME: number = 1;
const MAX_LENGTH_NAME: number = 50;
const MIN_LENGTH_CONTENT: number = 1;
const MAX_LENGTH_CONTENT: number = 1024;

export const postSchema = {
  MIN_LENGTH_NAME,
  MAX_LENGTH_NAME,
  MIN_LENGTH_CONTENT,
  MAX_LENGTH_CONTENT,
};

export const DEFAULT_POST_TAKE: number = 10;
export const DEFAULT_COMMENT_TAKE: number = 100;
